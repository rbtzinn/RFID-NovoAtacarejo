<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        style="@style/TituloTela"
        android:text="Confirmar Saída"
        android:layout_gravity="center"
        android:textColor="@color/error_red"/>

    <TextView
        android:id="@+id/tvLogoutMsg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:text="Ao sair, será necessário importar a planilha novamente na próxima sessão. Deseja continuar?"
        android:textColor="@color/text_secondary"
        android:textSize="16sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="24dp">

        <Button
            android:id="@+id/btnCancelar"
            style="@style/BotaoBase"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:text="Cancelar"
            android:backgroundTint="@color/text_secondary"/>

        <Button
            android:id="@+id/btnSair"
            style="@style/BotaoPerigo"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="Sair"/>
    </LinearLayout>
</LinearLayout>