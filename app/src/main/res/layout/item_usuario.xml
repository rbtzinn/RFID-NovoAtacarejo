<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    android:padding="8dp"
    android:background="@drawable/bg_item_rounded">

    <TextView
        android:id="@+id/tvNomeUsuario"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Nome do Usuário"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#222"
        android:fontFamily="sans-serif-medium"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/tvTipoUsuario"
        android:layout_marginEnd="10dp"/>

    <TextView
        android:id="@+id/tvTipoUsuario"
        android:layout_width="wrap_content"
        android:layout_height="28dp"
        android:text="ADM"
        android:textStyle="bold"
        android:textSize="13sp"
        android:gravity="center"
        android:background="@drawable/bg_tipo_usuario_membro"
        android:textColor="#FFF"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        app:layout_constraintEnd_toStartOf="@+id/btnRemover"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginEnd="10dp"/>

    <ImageButton
        android:id="@+id/btnRemover"
        android:layout_width="38dp"
        android:layout_height="38dp"
        android:background="@drawable/btn_remover_bg"
        android:src="@drawable/ic_delete"
    android:contentDescription="Remover"
    android:padding="7dp"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintBottom_toBottomOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>
